<div class="albumTitle">
  <h1><%= I18n.t('views.album.edit_album') %></h1>
  <div class="albumLinks">
    <%= link_to I18n.t('views.photo.add_photo'), new_album_photo_path(@album) %> |
    <%= link_to I18n.t('views.preview'), album_photos_path(@album) %> |
    <%= link_to I18n.t('views.delete'), [current_user, @album], confirm: I18n.t('views.album.delete_msg'), :method => :delete %> |
  </div>
</div>

<%= render 'form' %>

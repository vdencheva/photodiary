  <div class="topHolder">
    <div class="topNavigation">
      <ul class="nav">
        <li><%= link_to "", root_path, :title => I18n.t('views.home'), :class => "topLogo" %></li>
        <li><%= link_to I18n.t('views.user.list'), users_path %></li>
        <% if logged_in? %>
          <li><%= link_to I18n.t('views.user.profile'), session[:current_user] %></li>
          <li><%= link_to I18n.t('views.album.my_albums'), user_albums_path(session[:current_user]) %></li>
          <li><%= link_to I18n.t('views.album.new_album'), new_user_album_path(session[:current_user]) %></li>
          <li><%= link_to I18n.t('views.user.logout'), logout_path, method: "delete" %></li>
        <% else %>
          <li><%= link_to I18n.t('views.user.registration'), new_user_path %></li>
          <li><%= link_to I18n.t('views.user.login'), login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>